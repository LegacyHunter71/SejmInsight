spring:
  application:
    name: SejmInsight

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:parliament_db}
    username: ${DB_USER:admin}
    password: ${DB_PASS:password}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        jdbc:
          batch_size: 25
          order_inserts: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KC_ISSUER_URL:http://localhost:8081/realms/parliament-realm}

  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null
    deserialization:
      fail-on-unknown-properties: false

server:
  port: ${SERVER_PORT:8080}
  forward-headers-strategy: framework

parliament:
  url: ${SEJM_API_URL:https://api.sejm.gov.pl/sejm/}
  term: ${SEJM_API_TERM:term10/MP}
  iam:
    keycloak: # I ten
      server-url: ${KC_SERVER_URL:http://localhost:8081}
      realm: ${KC_REALM:parliament-realm}
      admin-client-id: ${KC_ADMIN_CLIENT_ID:parliament-admin-client}
      admin-client-secret: ${KC_ADMIN_CLIENT_SECRET:kSLkQ0oE8I2w11TdOnAMZ0NBf1JVuYFk}
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.parliament: DEBUG